# 新旧记忆管理系统对比报告

## 系统概览

| | 旧系统 (文本记忆) | 新系统 (Memory Engine) |
|---|---|---|
| **架构** | 纯文本文件（§ 分隔） | FAISS 向量索引 + SQLite 元数据 |
| **模型** | 无 | BAAI/bge-small-zh-v1.5（512 维，92MB） |
| **检索方式** | 关键词匹配 / 全文扫描 | 语义向量检索 + 类别加权 |
| **入库方式** | 手动追加文本 | 文本 + 向量双写 |
| **分块策略** | 人工 § 分隔 | 智能分块（256-512 tokens） |
| **存储路径** | `~/.hermes/memories/MEMORY.md` | `~/.hermes/memories/memory.db` + `faiss.index` |

## 核心区别

### 1. 检索能力

| 能力 | 旧系统 | 新系统 |
|------|--------|--------|
| 语义理解 | ❌ 只匹配关键词 | ✅ 理解中文语义 |
| 多跳推理 | ❌ 不支持 | ✅ 支持 interleave |
| 按类别过滤 | ❌ 不支持 | ✅ 8 个分类 |
| 按相关性排序 | ❌ 无 | ✅ 相似度分数 |
| 模糊查询 | ❌ 必须精确匹配关键词 | ✅ 自然语言查询 |

**实际效果对比**：

| 查询 | 旧系统 | 新系统 |
|------|--------|--------|
| "Exec 审批超时怎么回事？" | ❌ 无结果（无"超时"关键词） | ✅ score=1.14，精确命中 |
| "安全问题有哪些？" | ⚠️ 返回安全规则（误匹配） | ✅ score=0.98，返回维护日志 |
| "用的什么模型？" | ❌ 无结果 | ✅ score=0.72，精确命中 |
| "BrowserWing 端口" | ✅ 关键词匹配 | ✅ score=0.94，精确命中 |

### 2. 资源消耗

| 指标 | 旧系统 | 新系统 | 变化 |
|------|--------|--------|------|
| MEMORY.md 行数 | 326 行 | 44 行 | ↓ 87% |
| 文件大小 | 15.4 KB | 2.1 KB | ↓ 86% |
| 每次会话 token | ~2521 | ~637 | ↓ 75% |
| 可检索条目 | 1 条（全文） | 64 条 | ↑ 64x |
| 模型加载延迟 | 0ms | 首次 ~1600ms（冷启动） | — |
| 后续检索延迟 | — | ~3ms | 极快 |

### 3. 记忆管理

| 能力 | 旧系统 | 新系统 |
|------|--------|--------|
| 自动策展 | ❌ 手动整理 | ✅ 自动优先级排序 |
| 去重检测 | ❌ 无 | ✅ 自动去重 |
| 统计面板 | ❌ 无 | ✅ 分类分布图 |
| 增量入库 | 仅文本追加 | 文本 + 向量双写 |
| 持久化 | ✅ 文本 | ✅ SQLite + FAISS |

## 性能测试

**测试环境**：Apple M4 Mac，32GB 统一内存
**数据集**：64 条记忆，8 个分类
**模型**：BAAI/bge-small-zh-v1.5（本地缓存，离线加载）

### 检索延迟

| 查询场景 | 延迟 | 相关度 |
|----------|------|--------|
| 身份查询 | 1667ms（冷启动） | 0.54 |
| 安全规则 | 3ms | 0.97 |
| 安全问题 | 3ms | 0.42 |
| 工具路径 | 3ms | 0.94 |
| 项目状态 | 3ms | 0.87 |
| 已知问题 | 3ms | 1.14 |
| 模型配置 | 3ms | 0.72 |
| BrowserWing | 3ms | 0.94 |

- **平均延迟**：211ms（含冷启动） / 3ms（热启动）
- **中位延迟**：3ms
- **平均相关度**：0.817

### Token 节省计算

```
旧系统: 326 行 × ~7.7 tokens/行 ≈ 2521 tokens/会话
新系统: 44 行 × ~14.5 tokens/行 ≈ 637 tokens/会话
节省: 1884 tokens/会话 ≈ 75%

按每天 100 轮对话计算：
每天节省: 188K tokens
每月节省: 5.6M tokens
```

## 优缺点分析

### 旧系统

**优点**：
- 零依赖，不需要任何外部服务
- 零延迟（文本已在 prompt 中）
- 人类可直接编辑
- 可靠性极高（纯文本文件）

**缺点**：
- 无法语义搜索，只能关键词匹配
- 记忆量增长后检索效率急剧下降
- 无法自动去重和策展
- 全文加载浪费大量 token
- 不支持多跳推理

### 新系统

**优点**：
- 语义理解，自然语言查询
- 检索精度大幅提升（0.82 平均相关度）
- 自动策展、去重、分类
- MEMORY.md 瘦身 87%，节省 75% token
- 支持多跳推理和类别过滤
- 热启动延迟仅 3ms

**缺点**：
- 需要加载 embedding 模型（首次 ~1.6s）
- 依赖 Python 环境和 FAISS
- 双写机制增加复杂度
- 模型损坏会导致检索不可用
- 需要维护数据库和索引文件

## 架构决策：为什么文本 + 向量双写

| 维度 | 纯文本 | 纯向量 | 双写 |
|------|--------|--------|------|
| 开机自检 | ✅ 0 延迟 | ❌ 需要查询 | ✅ 0 延迟 |
| 语义检索 | ❌ 不支持 | ✅ 支持 | ✅ 支持 |
| 可靠性 | ✅ 纯文本 | ❌ 依赖模型 | ✅ 文本兜底 |
| 可编辑性 | ✅ 直接编辑 | ❌ 需要工具 | ✅ 可编辑 |
| 维护成本 | 低 | 中 | 中 |

**结论**：双写是可靠性和功能性的最佳平衡。文本层保证系统启动时就有基础记忆（我是谁、用户是谁、规则是什么），向量层提供按需扩展的深度检索。

## 当前状态

- ✅ MEMORY.md 已切换为精简版（44 行）
- ✅ Memory Engine 已就绪（64 条，8 分类）
- ✅ 语义检索可用（平均相关度 0.82）
- ✅ 离线模型加载（无需网络）
- ⚠️ 语义检索尚未集成到 OpenClaw 对话流程中（需要添加 search 动作）

## 下一步

1. 在 `memory_tool.py` 中添加 `search` 动作，让对话中可以直接语义检索
2. 在 heartbeat 中集成自动同步和策展
3. 长期：考虑用更轻量模型或量化版本加速首次加载
